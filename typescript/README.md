# Typescript Ultrasound Sandbox

So far we just have a beamform profile configurator, for generating chebyshev like profiles.

Host the website with:

```bash
bun run dev:web
```

Or build with:

```bash
bun run build
```